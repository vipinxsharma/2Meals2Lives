NEXT_PUBLIC_STRAPI_URL=https://cms.2meals2lives.org
NEXT_PUBLIC_SITE_URL=https://2meals2lives.org

# Required only when the Next.js app starts handling /api/subscribers (currently
# the static site posts directly to Strapi).
# RESEND_API_KEY=

# Required only when this app gains write access to Strapi (e.g. for
# moderation flows). Currently public reads only. Keep empty until then.
# STRAPI_API_TOKEN=
